# Palette. Placeholder values — no theme chosen yet. Everything that takes a
# colour references these, so retheming is an edit here plus waybar/style.css.
$accent   = rgba(7aa2f7ee)
$inactive = rgba(414868aa)
$bg       = rgb(1a1b26)
$fg       = rgb(c0caf5)
